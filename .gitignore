# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# macOS AppleDouble resource forks
._*
**/._*

# Source research documents — reference material for site copy, not part of the site.
# Kept outside public/ so `vite build` never copies them into dist/ and they are
# never published by `npm run deploy`.
research-notes/
public/information/*.docx

# Claude Code machine-local settings — personal permission grants, not shared.
# (the existing *.local rule does not match *.local.json)
.claude/settings.local.json
.claude/settings.json
